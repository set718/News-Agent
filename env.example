# Google Alert 邮件处理系统 - 环境变量配置示例
# 请复制此文件为 .env 并填入实际的配置信息

# Gmail API 配置方式1: 使用环境变量（推荐，更安全）
# 从 Google Cloud Console 获取以下信息
GMAIL_CLIENT_ID=your_gmail_client_id_here
GMAIL_CLIENT_SECRET=your_gmail_client_secret_here
GMAIL_AUTH_URI=https://accounts.google.com/o/oauth2/auth
GMAIL_TOKEN_URI=https://oauth2.googleapis.com/token
GMAIL_REDIRECT_URIS=http://localhost

# Gmail 访问令牌（首次认证后自动生成）
# GMAIL_ACCESS_TOKEN={"token":"...","refresh_token":"..."}
# 设置为true使用环境变量存储token，false使用文件存储
GMAIL_USE_ENV_TOKEN=false

# Gmail API 配置方式2: 使用文件（备用方式）
# GMAIL_CREDENTIALS_FILE=credentials.json
# GMAIL_TOKEN_FILE=token.json
# GMAIL_API_KEY=your_gmail_api_key_here

# DeepSeek API 配置
# 从 https://platform.deepseek.com/ 获取 API Key
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# 数据库配置
# SQLite 数据库文件路径（默认即可）
DATABASE_URL=sqlite:///google_alerts.db

# 可选：使用 PostgreSQL 数据库
# DATABASE_URL=postgresql://username:password@localhost:5432/google_alerts

# 可选：使用 MySQL 数据库
# DATABASE_URL=mysql+pymysql://username:password@localhost:3306/google_alerts

# Langchain 配置
# 是否启用详细输出
LANGCHAIN_VERBOSE=false
# 内存类型: simple, conversation, summary
LANGCHAIN_MEMORY_TYPE=simple
# LLM 最大输出token数
LANGCHAIN_MAX_TOKENS=1000
# LLM 温度参数（创造性）
LANGCHAIN_TEMPERATURE=0.3